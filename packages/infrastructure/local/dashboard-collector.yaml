services:
  collector:
    command: ["--config=/etc/otelcol/config.yaml", "--config=/etc/otelcol/dashboard.yaml"]
    extra_hosts: ["host.docker.internal:host-gateway"]
    volumes:
      - ./collector-dashboard.yaml:/etc/otelcol/dashboard.yaml:ro
